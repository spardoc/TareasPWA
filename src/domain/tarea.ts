export class Tarea
{
    uid?: string;
    nombre?: string; // ?: indefinido, para que no me provoque una excepcion si no hay valores predefinidos
    nota?: string;
    fecha?: string;
    etiqueta?: string
}